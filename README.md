# Gateway - POC

### To Compile and Run
- `make`

### To Just Compile
- `make build`

### To Run with defaults
- `make run`

### CLI flags
- ./server -a {OPENFAAS_GATEWAY_ADDRESS} -p {LOCAL_LISTEN_PORT}
